server:
  port: 8080
  servlet:
    context-path: /api

spring:
  data:
    mongodb:
      host: quak-db
      port: 27017
      username: root
      password: ${MONGO_PASS}
      database: quak
      authentication-database: admin
      auto-index-creation: true